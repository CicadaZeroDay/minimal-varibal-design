# 📝 Настройка блога - Markdown система

## ✅ Что было сделано

Создана система для хранения статей блога в **Markdown формате** вместо TypeScript объектов.

## 📁 Структура

```
content/articles/
├── README.md                    # Инструкции
├── index.ts                     # Главный файл (гибридная система)
├── types.ts                     # TypeScript интерфейсы
├── legacy-articles.ts           # Старые статьи (для обратной совместимости)
├── legacy-helpers.ts            # Функции для работы со старыми статьями
└── *.md                         # Новые статьи в Markdown формате
```

## 🚀 Как добавить новую статью

### Шаг 1: Создайте Markdown файл

Создайте файл `content/articles/название-статьи.md`:

```markdown
---
title: "Название вашей статьи"
description: "Краткое описание для SEO и превью"
author: "NMC Team"
publishedAt: "25 January 2025"
category: "Case Studies"
tags: ["tag1", "tag2", "tag3"]
featuredImage: "/blog/image.jpg"
---

## Заголовок раздела

Текст статьи в Markdown формате...

### Подзаголовок

- Списки работают
- Обычно
- Как обычно

**Жирный текст** и *курсив* тоже поддерживаются.

> Цитаты тоже работают!

[Ссылки](https://example.com) тоже поддерживаются.
```

### Шаг 2: Сохраните и закоммитьте

Статья автоматически появится на сайте!

## 📊 Преимущества Markdown

✅ **Простота** - легко писать и редактировать  
✅ **Версионирование** - все в Git  
✅ **SEO** - хорошо индексируется поисковиками  
✅ **Форматирование** - поддержка всех основных элементов  
✅ **Не требует программирования** - можно писать в любом редакторе  

## 🔄 Миграция существующих статей

Существующие статьи остаются в `legacy-articles.ts` для обратной совместимости. 

Чтобы мигрировать статью в Markdown:

1. Скопируйте контент из `legacy-articles.ts`
2. Конвертируйте HTML в Markdown (или оставьте как есть, система поддерживает оба формата)
3. Создайте `.md` файл с frontmatter
4. Удалите из `legacy-articles.ts` (опционально)

## 🛠 Технические детали

- **Парсинг**: `gray-matter` для frontmatter
- **Конвертация**: `remark` + `remark-html` для Markdown → HTML
- **Загрузка**: Vite `import.meta.glob()` для динамической загрузки
- **Кеширование**: Статьи кешируются после первой загрузки

## 📝 Пример полной статьи

См. `content/articles/beauty-salon-case-study.md` для примера.

## ❓ Вопросы?

Если что-то не работает, проверьте:
1. Правильность frontmatter (YAML формат)
2. Что файл сохранен как `.md`
3. Что файл находится в `content/articles/`

